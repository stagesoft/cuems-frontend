{
  "_comment": "Leave websocketBaseUrl empty to derive the host from the page URL (the controller serving this UI). Set it explicitly (e.g. ws://10.16.10.2) only to point a dev UI at a remote controller.",
  "websocketBaseUrl": ""
}
